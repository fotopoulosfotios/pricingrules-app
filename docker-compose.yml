services:
  auditor:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: audit-app
    restart: unless-stopped
    ports:
      - "8501:8501"
    volumes:
      - ./storage:/app/storage




